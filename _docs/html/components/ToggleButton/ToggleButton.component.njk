{% from 'macros/icon.njk' import icon %}

{% macro ToggleButton(
  settings = false,
  classList = "",
  content = {
  initIcon: 'navicon',
  closeIcon: 'close',
  initText: false,
  closeText: false
}) %}

  <button class="c-toggle-button {{ classList }}" data-module="Toggle"
      {% if settings %}
        data-options='{{ settings }}'
      {% endif %}>

    <div class="c-toggle-button__init">
      {{ icon(content.initIcon, 'c-toggle-button-icon--init') }}
      {% if content.initText %}
        <span class="c-toggle-button__text">{{ content.initText }}</span>
      {% endif %}
    </div>

    <div class="c-toggle-button__close">
      {{ icon(content.closeIcon, 'c-toggle-button-icon--close') }}
      {% if content.closeText %}
        <span class="c-toggle-button__text">{{ content.closeText }}</span>
      {% endif %}
    </div>

  </button>

{% endmacro %}
